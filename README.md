# 小灵

小灵是一个 macOS 桌面小狐狸 AI 助手。

## 下载使用

打开 GitHub 仓库的 Releases 页面，下载最新版本中的 `Xiaoling-*-arm64.dmg` 安装包，双击打开后将“小灵”拖入 Applications 即可使用。

如果 macOS 提示应用来自未认证开发者，请在 Finder 中右键“小灵”，选择“打开”。

## 本地开发

```bash
npm install
npm run dev
```

## 本地打包

```bash
npm run dist:mac
```
